import React from 'react'
import ReactDOM from 'react-dom'

import App from './components/App'

// render the App component into the element <div id="root"></div>
ReactDOM.render(<App />, document.getElementById('root'))
